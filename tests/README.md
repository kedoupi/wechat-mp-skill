# Tests

## Offline

```bash
bash tests/run.sh
```

Fixtures: `fixtures/sample.md` (公众号-style markdown for preview / md2html).

## Live / manual

See `docs/README.md` (Golden-path demo) for a path using a **user project** output dir.
Do not store generated previews under `tests/`.
